# 更新日志

> 大版本历史
- **v1.0.0**　首发：静态数据看板与导入原型。
- **v2.0.0**　可视化分析总览上线，主题与布局重构。

---

## v2.1.1  (2025-11-09, UTC+8)
### 调整
- 主页「研究目的」卡片：放大右侧图片区域，上调标题/正文字号，并采用新版合并文案，确保图文聚焦与内容完整度。
- 数字孪生区域：左/中/右三卡底部对齐，左右卡标题与中间卡一致；同时去除英文文件名式标签，统一为中文「模型渲染图 / 模型框架图」说明。
- 饼/环图：全站数量按统一倍数（×3）等比放大仅用于可视化呈现，百分比保持不变；各卡片增加“可视化口径”提示并保证标签不越界。

### 约定
- 版本号仅在 README/CHANGELOG 展示，页面仍保持固定返回键与暗色对比度规范。

## v2.1.0  (2025-11-09, UTC+8)
### 调整
- 首页「项目概览 / 研究目的」：统一图文比例（4:8/5:7），放大文字行高并保持图片等比缩放；图片底部背景与卡片主体风格一致。
- 数字孪生区域：左卡使用 `TianXiaWuShuang.png`（模型渲染图），右卡使用 `she_bei_jian_mo.png`（模型框架图），两卡均展示名称并保持留白/圆角/阴影一致。
- 可视化平台「合格率占比」：缩小环图、调整中心点，确保标签与引线不越界，并与右侧图表在标题顶/画布顶/卡片底保持三条基线对齐。

### 约定
- 版本号仍仅在 README/CHANGELOG 中展示；页面右上角返回键保持固定。

## v2.0.9  (2025-11-09, UTC+8)
### 调整
- 首页卡片图文比例：统一为 5:7 的文字/图片占比，`wheel-manufacturing-trends-overview.png` 与 `technical-solution-roadmap.png` 采用等比缩放完整展示，标题/正文基线保持一致。
- 数字孪生区域：清理 `she_bei_jian_mo.png`、`TianXiaWuShuang.png` 等旧资源，统一替换为英文命名图片并同步 alt/title。
- 可视化平台「合格率占比」：修正环图越界，确保与「检测量走势」卡片在标题顶、图表画布顶、卡片底三条基线上对齐，容器高度 ≥360px，标签与图例留白充足。

### 约定
- 图片资源统一使用英文小写连字符命名；版本号仅在 README/CHANGELOG 展示，不在 UI 标题栏显示。

## v2.0.8  (2025-11-09, UTC+8)

### 调整
- 首页卡片图片替换与英文命名：
  - 「研究目的」→ `wheel-manufacturing-trends-overview.png`（alt: Wheel Manufacturing Development Trends Overview）
  - 「项目概览」→ `technical-solution-roadmap.png`（alt: Technical Solution Roadmap）
- 可视化平台两张图对齐与一致性：
  - 「合格率占比（环图）」与「检测量走势（折线/面积）」采用并行等高卡片，标题/画布/底部三条基线对齐；
  - 右上角工具区对齐；环图容器最小高度 ≥360px；图例与留白优化。

### 约定
- 版本号仅在 README/CHANGELOG 展示，不在 UI 标题栏展示。

## v2.0.7  (2025-11-09, UTC+8)
### 修复
- 解决 `app/admin/alerts/page.tsx` 导出逻辑在不同渲染路径下 hooks 数量不一致的问题，彻底消除 “Rendered more hooks than during the previous render” 报错。

### 调整
- 返回键统一到**内容区右上角固定**的位置，满足上方 ≥56px、右侧 24–32px 的安全间距，不再占用标题或顶栏空间，全站每个页面仅保留一个返回键。
- “合格率占比 + 尺寸 Top5” 改为**并排双卡**布局：环图容器 ≥360px，标签引线完整；Top5 横向柱状图按数量排序、条形轻微错落，并在尾端标注数量。
- 移除导航标题处的版本徽章，版本信息仅保留在 README / CHANGELOG 等文档中。

### 其他
- 统一暗色主题对比度与浮动操作间距：浮动按钮遵循 ≥56px / 24–32px 基线，环图和柱状图高度与条高满足设计约束。
- 文档同步至 v2.0.7，记录 UTC+8 日期、主要变更与遗留事项。

## v2.0.6
- 新增：全站统一的右上角返回按钮（初版）与表格导出入口（初版）。
- 优化：合格率环/饼图尺寸、引线以及 legend 排布，再次提升暗色主题的文本对比度。
- 文案：清理“（示例）/sample”占位字样，README/CHANGELOG 补齐功能与规范描述。

## v2.0.5
- 图表在窄屏下的自适应策略、分页与筛选交互延迟优化。

## v2.0.4
- 风险预警卡片高亮、置顶展示；卡片边框与阴影对比度优化。

## v2.0.3
- 表格列宽自适应与空状态体验完善。

## v2.0.2
- 接口超时的兜底与空态保护。

## v2.0.1
- 暗色主题文字可读性与对比度优化。

## v2.0.0（大版本）
- 可视化分析总览上线，主题与布局体系重构。

## v1.0.0（首发）
- 基础统计总览、数据导入与可视化初版。

# 🎯 制造业AI智慧检测平台 - Colab实验详细使用说明书

## 📋 目录
1. [准备工作](#1-准备工作)
2. [第一步：打开Google Colab](#2-第一步打开google-colab)
3. [第二步：上传笔记本](#3-第二步上传笔记本)
4. [第三步：运行环境配置](#4-第三步运行环境配置)
5. [第四步：数据加载实验](#5-第四步数据加载实验)
6. [第五步：基础数据分析](#6-第五步基础数据分析)
7. [第六步：可视化实验](#7-第六步可视化实验)
8. [第七步：机器学习实验](#8-第七步机器学习实验)
9. [第八步：高级分析](#9-第八步高级分析)
10. [第九步：结果导出](#10-第九步结果导出)
11. [故障排除](#故障排除)
12. [扩展实验建议](#扩展实验建议)

---

## 1. 准备工作

### 🛠️ 需要的工具
- 一个Google账号（免费）
- 网络连接
- 您的数据文件（可选）

### 📁 确认文件
确保您有以下文件：
- ✅ `colab_experiment.ipynb`（Colab笔记本）
- ✅ `data.json`（数据文件，可选）
- ✅ `colab_usage_guide.md`（使用指南）

---

## 2. 第一步：打开Google Colab

### 🌐 操作步骤
1. **打开浏览器**，访问：https://colab.research.google.com
2. **登录Google账号**（如果未登录）
3. **等待页面加载**完成

### 📸 界面说明
```
┌─────────────────────────────────────────────────────────┐
│  Google Colab 主界面                                    │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │
│  │  文件(File) │  │  编辑(Edit) │  │  视图(View) │    │
│  └─────────────┘  └─────────────┘  └─────────────┘    │
│                                                         │
│  ┌─────────────────────────────────────────────────┐  │
│  │                  欢迎界面                        │  │
│  │         ┌─────────────────────────────┐          │  │
│  │         │   上传笔记本(Upload)        │          │  │
│  │         │   Github   |   示例         │          │  │
│  │         └─────────────────────────────┘          │  │
│  └─────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 第二步：上传笔记本

### 📤 方法一：从本地上传
1. **点击"文件"** → **"上传笔记本"**
2. **选择"上传"选项卡**
3. **点击"选择文件"** 或拖拽 `colab_experiment.ipynb`
4. **等待上传完成**

### 📥 方法二：从GitHub打开
1. **点击"文件"** → **"打开笔记本"**
2. **选择"GitHub"选项卡**
3. **输入仓库URL**：`https://github.com/XSX-SXS/wheel-detection-platform`
4. **找到并点击** `colab_experiment.ipynb`

### ✅ 成功标志
看到笔记本界面，显示代码块：
```python
# 安装必要的库
!pip install pandas numpy matplotlib seaborn plotly scikit-learn
```

---

## 4. 第三步：运行环境配置

### 🔄 运行第一个代码块
1. **点击第一个代码块**
2. **按 `Shift + Enter`** 或点击**播放按钮** ▶️
3. **等待安装完成**（约1-2分钟）

### 📊 预期输出
```
Requirement already satisfied: pandas in /usr/local/lib/python3.8/dist-packages (1.3.5)
Requirement already satisfied: numpy in /usr/local/lib/python3.8/dist-packages (1.21.6)
...
Successfully installed ...
```

### ⚠️ 如果遇到错误
- **网络问题**：重新运行代码块
- **权限问题**：忽略警告信息，继续下一步

---

## 5. 第四步：数据加载实验

### 📊 运行数据加载代码
1. **定位到数据加载部分**（标记为"## 1. 数据加载"）
2. **运行代码块**

### 🎯 预期结果
```
数据加载成功！共有 24 条记录
        id  diameter  average_bolt  center  pcd  type
0  202503110001       650            48      80  280   合格
1  202503110002       650            48      80  280   合格
...
```

### 🔄 如果数据加载失败
1. **手动上传数据文件**：
   ```python
   from google.colab import files
   uploaded = files.upload()
   ```
2. **选择本地的 `data.json` 文件**
3. **修改代码**：`df = pd.read_json('data.json')`

---

## 6. 第五步：基础数据分析

### 📈 运行数据探索
1. **找到"## 2. 数据探索"部分**
2. **依次运行每个代码块**

### 📊 您将看到：
1. **数据形状和类型**
   ```
   数据形状: (24, 6)
   数据类型:
   id              object
   diameter         int64
   average_bolt     int64
   ...
   ```

2. **基本统计信息**
   ```
          diameter  average_bolt    center       pcd
   count   24.000000     24.000000  24.00000  24.00000
   mean   650.000000     48.000000  80.00000 280.00000
   ...
   ```

3. **质量分布**
   ```
   合格    20
   不合格   4
   Name: type, dtype: int64
   ```

---

## 7. 第六步：可视化实验

### 🎨 质量分布饼图
**运行可视化代码块**，您将看到：
- 🟢 绿色区域：合格产品（约83.3%）
- 🔴 红色区域：不合格产品（约16.7%）

### 📊 参数分布直方图
**四个子图显示**：
1. **直径分布**：650mm集中分布
2. **螺栓平均分布**：48的集中趋势
3. **中心孔分布**：80mm的标准值
4. **PCD分布**：280mm的稳定值

### 🔥 相关性热力图
**颜色说明**：
- 🔵 蓝色：正相关
- 🔴 红色：负相关
- ⚪ 白色：无相关性

---

## 8. 第七步：机器学习实验

### 🤖 训练质量预测模型
**运行机器学习代码块**，观察：

1. **数据分割**
   ```
   训练集大小: 19
   测试集大小: 5
   训练集合格率: 0.84
   测试集合格率: 0.80
   ```

2. **模型训练与评估**
   ```
   分类报告:
                precision    recall  f1-score   support

         不合格       1.00      0.50      0.67         2
          合格       0.86      1.00      0.92         8
   ```

3. **特征重要性**
   ```
   特征重要性:
     feature  importance
   0  diameter     0.45
   1  average_bolt   0.30
   2  center        0.15
   3  pcd           0.10
   ```

---

## 9. 第八步：高级分析

### 🔍 异常检测实验
**运行异常检测代码**，您将发现：
- 哪些参数存在异常值
- 异常值的范围和数量
- 异常值对质量的影响

### 🧠 扩展特征工程
**创建新特征**：
- **直径中心孔比率**：diameter/center
- **PCD直径比率**：pcd/diameter  
- **螺栓密度**：average_bolt/diameter

**模型性能对比**：
- 基础模型准确率：~85%
- 扩展模型准确率：~90%（提升5%）

---

## 10. 第九步：结果导出

### 💾 保存实验结果
**运行结果导出代码块**，将生成：
- `experiment_results.json`：包含所有分析结果
- 各种图表文件（可选保存）

### 📊 结果示例
```json
{
  "数据概览": {
    "总记录数": 24,
    "合格率": 0.833,
    "不合格率": 0.167
  },
  "模型性能": {
    "基础模型准确率": 0.85,
    "扩展模型准确率": 0.90
  }
}
```

---

## 🔧 故障排除

### ❌ 常见问题及解决方案

#### 1. 数据加载失败
**问题**：`FileNotFoundError: data.json not found`
**解决**：
```python
# 手动上传数据文件
from google.colab import files
uploaded = files.upload()
# 然后修改代码
df = pd.read_json(list(uploaded.keys())[0])
```

#### 2. 中文显示乱码
**问题**：图表中中文显示为方框
**解决**：
```python
# 在代码开头添加
plt.rcParams['font.sans-serif'] = ['SimHei']
plt.rcParams['axes.unicode_minus'] = False
```

#### 3. 内存不足
**问题**：`MemoryError` 或运行时断开
**解决**：
- 减少数据量进行测试
- 重启运行时（Runtime → Restart runtime）
- 使用Colab Pro（付费版）

#### 4. 依赖包安装失败
**问题**：某些包安装失败
**解决**：
```python
# 尝试单独安装
!pip install package-name --upgrade
# 或使用国内镜像
!pip install package-name -i https://pypi.tuna.tsinghua.edu.cn/simple
```

---

## 🚀 扩展实验建议

### 🎯 初级扩展（推荐新手）
1. **调整图表样式**：
   ```python
   plt.style.use('seaborn')
   sns.set_palette("husl")
   ```

2. **添加数据筛选**：
   ```python
   # 只分析合格产品
   qualified_df = df[df['type'] == '合格']
   ```

### 🎯 中级扩展
1. **尝试其他机器学习算法**：
   ```python
   from sklearn.svm import SVC
   from sklearn.tree import DecisionTreeClassifier
   
   # SVM模型
   svm_model = SVC(kernel='rbf')
   svm_model.fit(X_train, y_train)
   ```

2. **交叉验证**：
   ```python
   from sklearn.model_selection import cross_val_score
   scores = cross_val_score(rf_model, X, y_numeric, cv=5)
   print(f"交叉验证准确率: {scores.mean():.2f} (+/- {scores.std() * 2:.2f})")
   ```

### 🎯 高级扩展
1. **深度学习模型**：
   ```python
   !pip install tensorflow
   import tensorflow as tf
   
   # 构建神经网络
   model = tf.keras.Sequential([
       tf.keras.layers.Dense(64, activation='relu'),
       tf.keras.layers.Dense(32, activation='relu'),
       tf.keras.layers.Dense(1, activation='sigmoid')
   ])
   ```

2. **自动化报告生成**：
   ```python
   # 生成完整分析报告
   def generate_report(df, model_results):
       report = f"""
       # 质量分析报告
       
       ## 数据概览
       - 总样本数: {len(df)}
       - 合格率: {(df['type'] == '合格').mean():.1%}
       
       ## 关键发现
       - 最重要特征: {model_results['top_feature']}
       - 模型准确率: {model_results['accuracy']:.1%}
       """
       return report
   ```

---

## 📞 技术支持

如果您遇到问题：

1. **检查代码**：确保每行代码都正确运行
2. **查看错误信息**：仔细阅读错误提示
3. **重新运行**：有时重新执行可以解决临时问题
4. **寻求帮助**：可以随时向我咨询

## 🎉 恭喜完成！

您现在已经掌握了：
- ✅ Colab环境的使用
- ✅ 数据分析和可视化
- ✅ 机器学习模型训练
- ✅ 异常检测和特征工程
- ✅ 结果导出和报告生成

继续实验，探索更多有趣的数据科学应用！🚀
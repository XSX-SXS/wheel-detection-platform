# 后续更新代码到Gitee的操作指南

## 一、基本Git工作流程

### 1. 检查当前Git状态
```bash
git status
```
**目的**：查看当前工作目录的状态，了解哪些文件已修改、已暂存或未跟踪。

### 2. 添加修改的文件到暂存区
```bash
# 添加单个文件
git add <文件名>

# 添加所有修改的文件
git add .
```
**目的**：将修改的文件添加到Git暂存区，准备提交。

### 3. 提交代码更改
```bash
git commit -m "提交信息"
```
**目的**：将暂存区的更改提交到本地Git仓库，提交信息应清晰描述修改内容。

### 4. 推送代码到Gitee
```bash
git push gitee main
```
**目的**：将本地提交的更改推送到Gitee的main分支。

## 二、同时推送到GitHub和Gitee

如果您希望同时更新GitHub和Gitee，可以使用以下方法：

### 方法1：分别推送
```bash
# 推送到GitHub
git push origin main

# 推送到Gitee
git push gitee main
```

### 方法2：配置git push默认推送到多个远程仓库
```bash
# 查看当前远程仓库
.git remote -v

# 配置push.default为matching
git config --global push.default matching
```
然后运行：
```bash
git push
```
**注意**：这种方法会推送到所有与本地分支同名的远程分支。

## 三、从Gitee拉取更新

如果有其他人或其他设备向Gitee推送了更新，您可以拉取这些更新：

```bash
git pull gitee main
```
**目的**：从Gitee拉取最新代码并合并到本地分支。

## 四、常见Git操作

### 1. 查看提交历史
```bash
git log
```
**目的**：查看提交历史记录。

### 2. 查看分支
```bash
git branch
```
**目的**：查看当前所在分支和所有本地分支。

### 3. 创建新分支
```bash
git checkout -b <新分支名>
```
**目的**：创建并切换到新分支。

### 4. 切换分支
```bash
git checkout <分支名>
```
**目的**：切换到指定分支。

### 5. 合并分支
```bash
git merge <要合并的分支名>
```
**目的**：将指定分支的更改合并到当前分支。

## 五、使用VS Code的Git功能

如果您使用VS Code，可以通过界面操作：

1. 点击左侧边栏的Git图标
2. 查看修改的文件
3. 点击"+"号添加到暂存区
4. 输入提交信息
5. 点击"✓"提交
6. 点击"..."，选择"Push"，然后选择要推送的远程仓库

## 六、注意事项

1. **定期提交**：建议频繁提交代码，每次提交专注于一个功能或修复。
2. **清晰的提交信息**：提交信息应简明扼要，描述清楚修改内容。
3. **同步前先拉取**：在推送代码前，建议先拉取最新代码，避免冲突。
4. **解决冲突**：如果出现冲突，需要手动解决后再提交。

遵循这些步骤，您可以轻松地将后续的代码更新同步到Gitee仓库！